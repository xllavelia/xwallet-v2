
:root {
  --bg-deep: #080907; /* Почти черный, с легким зеленым оттенком */
  --glass-bg: rgba(255, 255, 255, 0.03);
  --glass-border: rgba(238, 245, 178, 0.1);
  --text-main: #f4f4f4;
  --text-muted: #6b6d65;
  --easing: cubic-bezier(0.22, 1, 0.36, 1);
}

* {
  box-sizing: border-box;
}

/* --- БАЗОВЫЙ КОНТЕЙНЕР (90vh) --- */
.wallet-canvas {
  position: relative;
  width: 100%;
  height: 90vh;
  background-color: #121212;
  overflow: hidden;
  font-family: "Unbounded", sans-serif;
  color: var(--text-main);
  /* Едва заметный шум/градиент на фоне */
}

/* --- КОЛЬЦА НА ФОНЕ (Масштабные, но ненавязчивые) --- */
.rings-background {
  position: absolute;
  top: 50%;
  left: 50%;
  width: 100vw;
  height: 100vw;
  max-width: 1200px;
  max-height: 1200px;
  transform: translate(-50%, -50%);
  z-index: 0;
  pointer-events: none;
  perspective: 1200px;
}

.ambient-flare {
  position: absolute;
  top: 40%;
  left: 60%;
  width: 500px;
  height: 500px;
  filter: blur(200px);
  opacity: 0.08;
  transform: translate(-50%, -50%);
  animation: breathe 8s ease-in-out infinite alternate;
}

.orbit {
  position: absolute;
  top: 50%;
  left: 50%;
  border-radius: 50%;
  border: 1px solid rgba(238, 245, 178, 0.08); /* Очень тусклые */
  transform-style: preserve-3d;
}

/* Индивидуальные орбиты с 3D вращением */
.orbit-1 {
  width: 60%;
  height: 60%;
  margin-top: -30%;
  margin-left: -30%;
  border: 1px dashed rgba(238, 245, 178, 0.15);
  animation: spin3D 45s linear infinite;
}

.orbit-2 {
  width: 85%;
  height: 85%;
  margin-top: -42.5%;
  margin-left: -42.5%;
  animation: spin3D 60s linear infinite reverse;
}

.orbit-3 {
  width: 110%;
  height: 110%;
  margin-top: -55%;
  margin-left: -55%;
  border: 2px solid rgba(238, 245, 178, 0.03);
  animation: spin3D 90s linear infinite;
}

@keyframes spin3D {
  0% { transform: rotateX(65deg) rotateY(15deg) rotateZ(0deg); }
  100% { transform: rotateX(65deg) rotateY(15deg) rotateZ(360deg); }
}

/* --- РАСПРЕДЕЛЕННЫЙ КОНТЕНТ --- */
.canvas-content {
  position: relative;
  z-index: 10;
  width: 100%;
  height: 100%;
  padding: 4vw 6vw;
  display: flex;
  flex-direction: column;
}



.mono-text { color: var(--text-muted); }
.highlight { color: var(--xlavelia); }
.opacity-50 { opacity: 0.5; }

/* Центральная типографика */
.massive-typography {
  position: absolute;
  top: 45%;
  left: 6vw;
  transform: translateY(-50%);
}

.tech-eyebrow {
  font-family: 'Inter', sans-serif;
  font-weight: 600;
  font-size: 0.85rem;
  letter-spacing: 0.4em;
  color: rgba(255, 255, 255, 0.4);
  margin-bottom: 2rem;
}

.cursive-text {
  font-family: 'Yellowtail', cursive;
  color: var(--xlavelia);
  line-height: 0.9;
  margin: 0;
  text-shadow: 0 10px 40px rgba(238, 245, 178, 0.1);
}

.main-cursive {
  font-size: clamp(4rem, 8vw, 7rem);
  margin-left: -5px;
}

.sub-cursive {
  font-size: clamp(4.5rem, 9vw, 8rem);
  padding-left: 10vw; /* Сдвиг вправо для композиции */
}

/* Парящая карточка баланса */
.glass-balance-card {
  position: absolute;
  bottom: 12vh;
  left: 6vw;
  background: var(--glass-bg);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid var(--glass-border);
  padding: 2rem;
  border-radius: 24px;
  width: 320px;
  box-shadow: 0 30px 60px rgba(0, 0, 0, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.1);
}

.card-label {
  font-size: 0.85rem;
  color: var(--text-muted);
  margin: 0 0 0.5rem 0;
}

.card-amount {
  font-size: 2.5rem;
  font-weight: 300;
  letter-spacing: -0.03em;
  margin: 0 0 2rem 0;
  color: #fff;
}

.card-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-top: 1px solid rgba(255,255,255,0.05);
  
}

.wallet-address {
  font-size: 0.75rem;
  color: rgba(255,255,255,0.5);
}

.badge {
  font-size: 0.65rem;
  background: rgba(238, 245, 178, 0.1);
  color: var(--xlavelia);
  padding: 4px 8px;
  border-radius: 4px;
  letter-spacing: 0.1em;
  font-weight: 600;
}

/* Текст в правом нижнем углу */
.bottom-corner-accent {
  position: absolute;
  bottom: 8vh;
  right: 6vw;
  text-align: right;
}

.marker-text {
  font-family: 'Permanent Marker', cursive;
  font-size: 1.8rem;
  color: var(--xlavelia);
  display: block;
  margin-bottom: 0rem;
  /* transform: rotate(-2deg); Легкий наклон для дерзости */
}

.description {
  font-size: 0.95rem;
  color: var(--text-muted);
  line-height: 1.6;
  max-width: 300px;
  margin: 0 0 0 auto;
}

/* --- АНИМАЦИИ (FRESH & WOW) --- */

/* Анимация появления из блюра */
.animate-reveal {
  opacity: 0;
  transform: translateY(30px);
  filter: blur(15px);
  animation: reveal-up 1.4s var(--easing) forwards;
}

.step-1 { animation-delay: 0.2s; }
.step-2 { animation-delay: 0.4s; }
.step-3 { animation-delay: 0.6s; }
.step-4 { animation-delay: 0.8s; }
.step-5 { animation-delay: 1.0s; }

@keyframes reveal-up {
  to {
    opacity: 1;
    transform: translateY(0);
    filter: blur(0);
  }
}

/* Медленное парение карточки */
.float-anim {
  animation: float 6s ease-in-out infinite;
}

@keyframes float {
  0% { transform: translateY(0px); }
  50% { transform: translateY(-15px); }
  100% { transform: translateY(0px); }
}

@keyframes pulse-dot {
  0% { box-shadow: 0 0 0 0 rgba(238, 245, 178, 0.7); }
  70% { box-shadow: 0 0 0 6px rgba(238, 245, 178, 0); }
  100% { box-shadow: 0 0 0 0 rgba(238, 245, 178, 0); }
}

@keyframes breathe {
  to { transform: translate(-50%, -50%) scale(1.2); opacity: 0.12; }
}

/* Адаптив, чтобы красота не сломалась на мобилках */
@media (max-width: 768px) {
  .hidden-mobile { display: none; }
  
  .massive-typography {
    top: 25%;
    transform: none;
  }
  
  .sub-cursive {
    padding-left: 5vw;
  }
  
  .glass-balance-card {
    position: relative;
    bottom: auto;
    left: auto;
    margin-top: 50vh;
    width: 100%;
  }
  
  .bottom-corner-accent {
    position: relative;
    bottom: auto;
    right: auto;
    text-align: left;
    margin-top: 0rem;
  }
  
  .description {
    margin: 0;
  }
}
